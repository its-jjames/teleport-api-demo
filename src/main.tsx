import React from 'react'
import ReactDOM from 'react-dom/client'
import './index.css'
import TeleportUploadPage from './pages/TeleportUploadPage'

ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
        <TeleportUploadPage />
    </React.StrictMode>,
)
